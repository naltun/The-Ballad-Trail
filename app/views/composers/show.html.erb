<h3><%= @composer.fname %> <%= @composer.lname %></h3>
<p><%= @composer.fname %></p>
<p><%= @composer.bio %></p>

<%= link_to "Edit", edit_composer_path(@composer)%>
<%= link_to "Destroy", composer_path(@composer), method: :delete, data: { confirm: "Are you sure?"} %>
